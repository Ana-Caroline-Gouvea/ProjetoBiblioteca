@model ProjetoBiblioteca.Models.Genero

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>Gênero</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="GeneroId" />
            <div class="form-group">
                <label asp-for="GeneroNome" class="control-label"></label>
                <input asp-for="GeneroNome" class="form-control" />
                <span asp-validation-for="GeneroNome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <button><a asp-action="Index">Voltar</a></button>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
